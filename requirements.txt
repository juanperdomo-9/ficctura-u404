# Django + lo que el proyecto realmente usa. (El requirements.txt viejo
# tenía ~15 paquetes de otro proyecto — whisper/transcripción de audio,
# ctranslate2, onnxruntime, etc. — colados por un venv compartido; se
# sacaron acá, no se usan en ningún lado de este código.)
Django==6.1
asgiref==3.12.1
sqlparse==0.5.5
python-dotenv==1.2.2
Pillow==12.3.0
requests==2.34.2
certifi==2026.7.22
charset-normalizer==3.4.9
idna==3.18
urllib3==2.7.0

# Producción (Render)
gunicorn==26.2.0
whitenoise==6.12.0
dj-database-url==3.1.2
psycopg2-binary==2.9.12
cloudinary==1.46.2
django-cloudinary-storage==0.3.0
